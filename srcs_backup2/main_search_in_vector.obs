#include <vector>
#include <iostream>

#include "search_in_vector.hpp"

int main()
{
    std::vector<int> vec;

    
    vec.push_back(10);
    vec.push_back(5);
    vec.push_back(7);
    vec.push_back(30);
    vec.push_back(2);
    vec.push_back(4);
    std::cout << search_in_vector(vec, 5) << std::endl;
    std::cout << search_in_vector(vec, 40) << std::endl;
}